# Contributing to iFly_Ctrl Documentation

本文只聚焦一件事：如何持续把 `iFly_Ctrl` 的 GitHub 文档写清楚、写准确、写得能长期维护。

## 文档贡献的目标

这个仓库的文档不应该只是“项目介绍”，而应该承担下面几类职责：

- 解释工程架构，而不是只罗列目录
- 解释技术实现，而不是只贴代码文件名
- 说明当前真实状态，而不是把“计划功能”写成“已实现功能”
- 帮后来者快速理解如何继续扩展工程
- 让 GitHub 首页就能承担一部分上手和维护说明

一句话概括：文档必须和代码一样，追求真实、可验证、可维护。

## 你可以贡献什么

欢迎补充以下内容：

- `README.md` 的架构说明、模块说明、构建说明
- `USB CDC`、`UART DMA`、`CAN`、`SBUS`、`CRSF` 等专题说明
- 代码阅读导图、时序图、数据流图
- CLI 使用示例与调参示例
- 文档中的错字、歧义、过期描述
- 新增模块接入后的说明章节

## 写文档前的基本原则

### 1. 先看代码，再写结论

任何文档内容，都应该优先基于实际代码确认，而不是凭印象推测。

推荐至少先确认下面几个维度：

- 模块入口在哪里
- 当前是否真的接入主流程
- 当前是否只是“已实现但未使用”
- 当前配置是不是测试模式
- 当前对外接口的真实名称是什么

例如本仓库里：

- `CAN` 服务已经写好，但当前主业务没有接入
- `CAN1` 当前配置成了 `loopback`
- `CLI` 默认传输通道是 `usb`
- `UART5` 已经接入 CLI，但其他 UART 只是初始化好了

这些都必须写清楚。

### 2. 区分“已实现”和“计划实现”

文档里最忌讳的一件事，就是把未来规划写成当前能力。

推荐明确使用下面三类表述：

- `已接入`
- `已实现，暂未接入`
- `计划中`

### 3. 多写“为什么”，少写“它是什么”

一个好文档不只解释“这个文件存在”，还要解释：

- 为什么要这样分层
- 为什么要用无锁队列
- 为什么要做双缓冲
- 为什么把业务逻辑放进 `iFly/`
- 为什么 `main.c` 只负责启动，不承担长期业务

### 4. 用代码中的真实名称

文档请尽量直接使用代码里的真实标识符，并用反引号包裹，例如：

- `FlightCtrlCli`
- `Shell`
- `ParameterManager`
- `SerialIoBase`
- `UartDmaService`
- `UsbCdcAcm`
- `CanService`

这样读者可以直接全文搜索定位到代码。

### 5. 说清楚限制条件

每个模块如果有边界或限制，都应该写出来。例如：

- 当前 `CAN1` 是 loopback 模式
- 当前 `SoftTimerService` 还没进主业务调度
- 当前 `CRSF / SBUS` 还没接入真实接收机路径
- 当前 CLI 默认密码是 `ifly`

## 推荐写作风格

建议遵循下面这些约定：

- 以中文叙述为主，关键接口名保留英文
- 先写整体，再写细节
- 先写真实状态，再写扩展方向
- 重要判断尽量给出代码锚点，例如模块名、文件路径、函数名
- 章节名尽量稳定，避免频繁改动目录结构
- 一段里只讲一件核心事情，避免一段塞太多结论

## README 应该承担什么职责

`README.md` 是仓库门面，建议长期保持下面这个结构：

1. 项目定位
2. 当前完成度
3. 仓库结构
4. 系统架构
5. 核心模块详解
6. 构建与使用
7. 文档贡献入口

不要把 README 写成下面这些东西：

- 只剩一句“这是一个飞控工程”
- 只罗列文件树，没有解释
- 大量复制源代码
- 把所有后续计划都堆在首页

README 应该负责“建立整体理解”，而不是替代所有详细文档。

## 推荐新增文档的方式

如果后续文档越来越多，建议逐步扩展为：

```text
docs/
├─ architecture/
├─ transport/
├─ control/
├─ protocols/
└─ examples/
```

当前仓库文档量还不大，所以先以根目录的 `README.md` 和 `CONTRIBUTING.md` 为主是合理的。

## 章节编写模板

如果你要给某个模块新增文档，推荐至少覆盖下面 6 个点：

### 1. 模块作用

回答这个模块“解决什么问题”。

### 2. 代码位置

说明它在仓库中的主要文件路径。

### 3. 启动入口或调用入口

说明它是如何被系统使用的。

### 4. 数据流或控制流

最好用列表、表格或 Mermaid 图把路径讲清楚。

### 5. 对外接口

列出关键类、结构体、命令或函数。

### 6. 当前限制

说明它现在做到哪一步，还没有做到哪一步。

## 推荐使用 Mermaid 图

对于下面这些内容，强烈建议优先画图：

- 启动流程
- UART DMA 收发路径
- USB CDC 数据路径
- CAN 发送与接收路径
- CLI 参数更新到 PID 的同步关系

图不需要花哨，但必须准确。

推荐原则：

- 图中节点名称尽量与代码一致
- 只画真实存在的路径
- 避免一张图塞太多层级

## 提交文档前的自检清单

在发起 PR 前，至少检查以下事项：

- 文档描述是否和当前代码一致
- 是否误把“预留接口”写成“已接入业务”
- 路径、类名、函数名是否拼写正确
- Markdown 标题层级是否清晰
- 表格和 Mermaid 是否在 GitHub 上能正常渲染
- 是否补充了必要的限制说明
- 是否删除了空泛表达和无依据判断

## 推荐的 PR 标题

可以参考下面这种风格：

- `docs: 完善 README 中的系统架构章节`
- `docs: 增加 USB CDC 数据通路说明`
- `docs: 修正文档中 CAN 模式描述`
- `docs: 补充 CLI 调参示例`

## 贡献时不要做的事

请尽量避免下面这些写法：

- 不看代码，直接根据经验补文档
- 把未来规划当作当前功能
- 复制大段 HAL 生成代码到 README
- 写一堆形容词，却没有任何结构信息
- 在没有验证的情况下写硬件连接或波特率
- 把和文档无关的代码重构混进纯文档 PR

## 关于截图、图片与外部资料

如果需要补充图片或截图，建议优先选择：

- 自己绘制的结构图
- 自己录制的终端截图
- 基于仓库代码整理出来的流程图

如果引用外部资料，请注意：

- 避免直接复制受版权保护内容
- 避免把第三方资料当成仓库自身实现
- 需要明确引用来源

## 最后建议

这个仓库最值得长期维护的，不只是代码本身，还有它作为“飞控底座”的知识结构。好的文档应该帮助后来者快速回答这几个问题：

- 工程怎么启动
- 模块怎么分层
- 数据怎么流动
- 现在做到哪一步
- 我下一步该从哪里接着做

如果一篇文档能把这五个问题回答清楚，那它就是有价值的贡献。
